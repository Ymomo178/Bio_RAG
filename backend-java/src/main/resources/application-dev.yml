spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/biorag}
    username: ${DB_USERNAME:biorag}
    password: ${DB_PASSWORD:biorag_dev}
  jpa:
    show-sql: false

logging:
  level:
    com.biorag.platform: DEBUG
